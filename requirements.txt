# Versioni installate nel venv del progetto (Python 3.11.6).
# Rigenerare dopo ogni aggiornamento dei pacchetti: le versioni qui devono
# combaciare con quelle usate per addestrare modello.pkl (scikit-learn in testa),
# altrimenti il pickle non si carica più.
#
# Rimossi perché non installati e non importati da nessun modulo:
# plotly, matplotlib, shinywidgets, jcamp, requests.

shiny==1.6.1
pandas==3.0.3
rdkit==2026.3.2
numpy==2.4.5
scikit-learn==1.8.0
